import { f } from "../helpers"
import type { Collection } from "../types"

export const workflowsCollections: Record<string, Collection> = {
    workflows: {
        section: "Workflows",
        description: "Workflows adalah salah satu collection pada setiap Room di Byon yang digunakan untuk mengatur tampilan (View) aplikasi Android/APK Byon.",
        long: "Workflows merupakan salah satu collection yang terdapat pada setiap Room di Byon. Collection ini berfungsi untuk melakukan setup tampilan (View) pada aplikasi Android/APK Byon. Setiap Room dapat memiliki lebih dari satu Workflows. Oleh karena itu, Workflows juga dapat disebut sebagai Module yang dimiliki oleh masing-masing Room atau Client.",
        meta: { documents: "1.2k", indexed: true },
        notes: [
            { kind: "note", text: "Workflows dapat diurutkan berdasarkan nilai `order` yang dimilikinya. Fungsi pengurutan ini hanya berlaku apabila seluruh Workflows menggunakan `order`. Apabila terdapat Workflows yang tidak memiliki `order`, maka nilai default-nya adalah 0 (urutan paling awal)." },
            { kind: "tip", text: "Hanya `_vsb` yang berstatus `ACTIVE` yang akan ditampilkan. Untuk menonaktifkannya, ubah statusnya menjadi `PAUSED`." },
            { kind: "tip", text: "Property `notifworker` sejauh ini harus diisi dengan angka `5`. Selain nilai tersebut, fitur ini tidak akan berfungsi." },
        ],
        fields: [
            f("_id", "string", true, "Unique document identifier"),
            f("name", "string", true, "Human-readable workflow name"),
            f("notifWorker", "number", false, "Field ini dapat dihapus. Namun, apabila ingin digunakan, wajib diisi dengan angka `5`."),
            f("order", "number", false, "Apabila Workflows ingin diurutkan, maka `order` pada seluruh Workflows menjadi wajib diisi. Jika tidak diisi, nilai default-nya adalah 0 (urutan paling awal)."),
            f("_vsb", "string", true, "Hanya dapat diisi dengan `ACTIVE` atau `PAUSED`. Jika `ACTIVE`, Workflows akan ditampilkan; jika `PAUSED`, Workflows tidak akan ditampilkan.", { enumValues: ["ACTIVE", "PAUSED"] }),
            f("nodes", "array", true, "Diisi dengan kumpulan object [[nodes]].", { of: "object" }),
            f("_class", "string", true, "Wajib diisi dengan `biz.byonchat.v2.services.workflow.models.Workflow`. Apabila tidak diisi dengan nilai tersebut, project Spring Boot tidak akan mendeteksi Workflows tersebut."),
        ],
        example: {
            _id: "exampleWorkflows",
            name: "This is Workflows Example",
            notifWorker: 5,
            _vsb: "ACTIVE",
            nodes: [
                { id: "form1", name: "Nodes Example 1", "...": "..." },
                { id: "form2", name: "Nodes Example 2", "...": "..." },
                { id: "form3", name: "Nodes Example 3", "...": "..." },
            ],
            _class: "biz.byonchat.v2.services.workflow.models.Workflow",
        },
        indexes: [
            { name: "_id", keys: ["_id"], unique: true },
        ],
        relations: [
            { field: "nodes", to: "nodes", kind: "references (many)" },
        ],
    },

    nodes: {
        section: "Workflows",
        description: "Nodes adalah child dari Workflows. Setiap Workflows dapat memiliki lebih dari satu Nodes.",
        long: "Nodes merupakan child dari Workflows. Setiap Workflows dapat memiliki lebih dari satu Nodes, dan setiap Nodes dapat saling terhubung satu sama lain melalui routing. Pada Team Business, Nodes biasa disebut sebagai Tab atau FORM. Selain Tab atau [[form|FORM]], terdapat juga [[validation|Validation]] yang termasuk sebagai Nodes. Nodes merupakan kekuatan utama (power) dari BYON, dengan poin utamanya terletak pada FORM yang dinamis serta cepat untuk di-setup.",
        meta: { documents: "4.8k", indexed: true },
        notes: [
            { kind: "note", text: "Nodes dapat diurutkan berdasarkan nilai `order` yang dimilikinya. Fungsi pengurutan ini hanya berlaku apabila seluruh Nodes menggunakan `order`. Apabila terdapat Nodes yang tidak memiliki `order`, maka nilai default-nya adalah 0 (urutan paling awal)." },
        ],
        fields: [],
        example: [
            {
                id: "nodeId",
                name: "Example Name",
                iconURL: "exampleIcon",
                order: 1,
                type: "FORM",
                "...": "...",
            },
            {
                id: "nodeId2",
                name: "Example Validation",
                type: "VALIDATION",
                "...": "...",
            },
        ],
        indexes: [
            { name: "_id", keys: ["_id"], unique: true },
        ],
        relations: [],
    },

    form: {
        section: "Workflows",
        description: "FORM adalah salah satu jenis Nodes pada Workflows yang berfungsi sebagai tampilan input yang dinamis.",
        long: "FORM merupakan salah satu jenis Nodes pada Workflows. FORM menjadi inti dari kekuatan BYON karena dapat di-setup secara dinamis dan cepat.",
        meta: { documents: "—", indexed: true },
        notes: [
            { kind: "tip", text: "Dari sekian banyak field pada schema FORM, tidak semuanya wajib dipakai. Apabila sebuah field tidak dibutuhkan, field tersebut dapat dihapus saja dari dokumen." },
            { kind: "note", text: "Nodes dapat diurutkan berdasarkan nilai `order` yang dimilikinya. Fungsi pengurutan ini hanya berlaku apabila seluruh Nodes menggunakan `order`. Apabila terdapat Nodes yang tidak memiliki `order`, maka nilai default-nya adalah 0 (urutan paling awal)." },
            { kind: "warn", text: "Setiap kali mengubah `routing` atau `assignment`, Anda wajib melakukan [[restart_jar|Restart Jar]] di server. Tanpa restart, perubahan tidak akan terbaca oleh project Spring Boot." },
            { kind: "note", text: "Flag `isHidden`, `hideForApp`, dan `hideForCms` mengontrol di mana form ditampilkan tanpa menghapusnya. Contoh: form khusus CMS → `hideForApp = true`; form khusus app → `hideForCms = true`; form disembunyikan sama sekali → `isHidden = true`." },
            { kind: "note", text: "Field `locRequired`, `locPreview`, `gmapsKeyRequired`, dan `locTitle` mengontrol fitur geolocation/peta di form. `locRequired = true` → history dari app akan menyertakan lokasi user; `locPreview = true` → preview peta ditampilkan sebelum submit berhasil; `locTitle` → teks label untuk field lokasi." },
            { kind: "note", text: "Contoh `submitFilters`: field `document` tidak boleh kosong; field `amount` harus > 1000; field `approval` harus sudah dipilih. Jika salah satu kondisi tidak terpenuhi saat submit, maka submit ditolak dengan pesan error yang sesuai." },
            { kind: "note", text: "Perbedaan perilaku `sfAnd`: jika `sfAnd = null` → logika OR (jika ada satu kondisi `submitFilters` tidak terpenuhi, kembalikan error kondisi tersebut). Jika `sfAnd` diisi pesan custom → logika AND (semua kondisi harus terpenuhi; jika semua terpenuhi, kembalikan pesan `sfAnd`)." },
            { kind: "note", text: "Contoh `btnFilters`: [user adalah manager, task amount > 5000] dengan `btnFilterAnd = true` (AND) → button \"Approve\" hanya ditampilkan jika user ADALAH manager DAN amount > 5000. Pada mode OR (`btnFilterAnd = false`), cukup salah satu kondisi terpenuhi." },
        ],
        fields: [
            // --- required ---
            f("_id", "string", true, "Unique document identifier"),
            f("name", "string", true, "LABEL dari FORM/VALIDATION"),
            f("type", "string", true, "Jenis Nodes. Hanya boleh diisi dengan `FORM` atau `VALIDATION`. Untuk dokumen FORM, diisi `FORM`.", {
                enumValues: ["FORM", "VALIDATION"],
            }),
            f("submitType", "string", true, "Menentukan perilaku tombol submit. Apabila tidak diisi, default-nya `SUBMIT`. `SUBMIT`: tombol submit biasa dan task langsung masuk ke history. `PREVIEW`: ketika tombol submit diklik, akan menampilkan preview/modal konfirmasi untuk memastikan kembali data yang dibutuhkan sudah benar.", {
                enumValues: ["SUBMIT", "PREVIEW"],
            }),
            f("fields", "array", true, "List field yang menyusun isi FORM. Detail tiap field dijelaskan pada [[fields]].", { of: "object", eg: { fields: [] } }),
            f("iconURL", "string", true, "Menampilkan ICON untuk UI Form di Android. Nilainya berupa URL."),
            f("routing", "object", true, "Object untuk menentukan node/form tujuan setelah task disubmit dari form saat ini."),
            f("defaultDest", "object", true, "Node tujuan default.", { depth: 1 }),
            f("node", "object", true, "Node tujuan ketika form disubmit. Bersifat nullable — apabila tujuannya END (selesai), diisi `null`.", { depth: 2 }),
            f("workflowId", "string", true, "Reference ke `workflows._id`.", { depth: 3 }),
            f("nodeId", "string", true, "Reference ke `nodes.id`.", { depth: 3 }),
            f("assignment", "object", true, "Object untuk menentukan siapa yang bisa mengakses/mengerjakan task di form ini. Detail dijelaskan pada [[assignment]].", { eg: { assignment: { type: "ALL" } } }),
            f("preview", "object", true, "Konfigurasi untuk menampilkan preview task di UI. Menentukan informasi apa saja yang ditampilkan ketika user melihat preview dari task sebelum membuka detail lengkap.", { eg: { preview: { title: "By : ${workflowResignImperial.form1.userInfo.name}", subtitle: "${workflowResignImperial.form1.formData.brand}" } } }),
            f("title", "string", true, "Judul preview task.", { depth: 1 }),
            f("subtitle", "string", true, "Subjudul preview task.", { depth: 1 }),
            f("searches", "array", false, "List pencarian data dari form lain untuk ditampilkan.", { depth: 1, of: "object", eg: { searches: [{ formId: "form1", key: "userInfo.name" }, { formId: "form1", key: "formData.nama_customer" }, { formId: "form1", key: "formData.serial_number" }] } }),
            f("formId", "string", true, "Reference ke `nodes.id`.", { depth: 2 }),
            f("key", "string", true, "Reference ke `fields.key`.", { depth: 2 }),
            f("customOffset", "object", false, "Custom offset untuk data tertentu.", { depth: 1 }),
            f("desc", "boolean", false, "Tampilkan deskripsi (`true` = ya).", { depth: 1 }),
            f("reportAccess", "object", true, "Fungsinya belum diketahui secara pasti. Yang pasti, field ini wajib ada dan selalu diisi dengan nilai `{ byUserGroups: { \"*\": \"ALL\" } }`."),
            f("byUserGroups", "object", true, "Selalu diisi dengan `{ \"*\": \"ALL\" }` — key `*` berarti semua user group, dan nilainya `ALL`.", { depth: 1 }),
            // --- optional ---
            f("order", "number", false, "Urutan tampil Nodes. Apabila tidak diisi, nilai default-nya adalah 0 (urutan paling awal)."),
            f("showBtns", "array", false, "List tombol tambahan (AdditionalButton) yang ditampilkan. Nilainya hanya boleh diisi dari daftar di samping. Yang umum dipakai adalah `CREATE_TASK`, yaitu menampilkan tombol + (new task). Dapat dikosongkan apabila tidak ingin menampilkan tombol +.", {
                of: "string",
                enumValues: ["CREATE_TASK", "CALENDAR_VIEW", "CALENDAR_VIEW_DEFAULT", "ASSIGNMENT_VIEW", "TICKET_TASK"],
                eg: { showBtns: ["CREATE_TASK"] },
            }),
            f("fieldsAfterSubmit", "array", false, "Isinya sama seperti [[fields]], tetapi field-field ini akan dijalankan dan nilainya didapatkan setelah form selesai disubmit.", { of: "object" }),
            f("submitFilters", "array", false, "List kondisi validasi yang dijalankan saat submit. Jika semua kondisi terpenuhi, submit berhasil; jika ada kondisi yang tidak terpenuhi, submit ditolak (reject) dengan pesan error. Struktur tiap object dijelaskan pada [[validationcondition]].", { of: "object", eg: { submitFilters: [{ op: "NOT_EQUALS", field: "${DATETIME|0.0.0.0.0.0|yyyy-MM-dd|${$.$.metadata.updateAt}|yyyy-MM-dd}", value: "${DATETIME|0.0.0.0.0.0|yyyy-MM-dd|${workflowVisitationAuto.form0.metadata.updateAt}|yyyy-MM-dd}", dataType: "DATE", dataFormats: ["yyyy-MM-dd"], errMessage: "Submit ditolak. Submit hanya dapat dilakukan pada tanggal ${DATETIME|0.0.0.0.0.0|yyyy-MM-dd|${workflowVisitationAuto.form0.metadata.updateAt}|yyyy-MM-dd}" }] } }),
            f("sfAnd", "string", false, "Submit filter AND — diisi dengan template message. Berfungsi menampilkan message (alert) ketika `submitFilters` berisi lebih dari satu object; gabungan pesannya ditampilkan di sini."),
            f("greenBadge", "boolean", false, "Menampilkan badge hijau di Nodes sebagai indikator status positif/selesai. Apabila tidak diisi, default-nya `false`."),
            f("redBadge", "boolean", false, "Menampilkan badge merah di Nodes sebagai indikator jumlah task pending/baru. Apabila tidak diisi, default-nya `false`."),
            f("canDraft", "boolean", false, "Mengaktifkan fitur save draft. Apabila tidak diisi, default-nya `true`. Jarang dipakai karena umumnya semua form sudah bisa menyimpan draft."),
            f("isHidden", "boolean", false, "Menyembunyikan form dari tampilan umum (hidden secara global). Default-nya `false`."),
            f("hideForApp", "boolean", false, "Menyembunyikan form khusus dari mobile app. Default-nya `false`."),
            f("hideForCms", "boolean", false, "Menyembunyikan form khusus dari CMS/web dashboard. Default-nya `false`."),
            f("locRequired", "boolean", false, "User harus mengisi lokasi sebelum submit task (wajib). Default-nya `false`. Jika `true`, history dari app akan menyertakan lokasi user."),
            f("locPreview", "boolean", false, "Tampilkan preview peta (Google Maps) di form. Default-nya `false`. Jika `true`, ketika user submit akan ditampilkan preview peta terlebih dahulu sebelum submit berhasil."),
            f("gmapsKeyRequired", "boolean", false, "Muat API key Google Maps untuk fitur peta. Default-nya `false`."),
            f("locTitle", "string", false, "Label/judul untuk input lokasi — teks label untuk field lokasi pada node saat ini."),
            f("byPassTaskFrom", "string", false, "Menyimpan ID FORM lain untuk di-bypass (dilewati) ketika status task adalah pending (status = 1). Hanya dipakai bila diperlukan; nilainya wajib berupa ID dari FORM lain."),
            f("calendarKey", "string", false, "Expression/template untuk mengekstrak tanggal task guna menampilkan di calendar view. Sangat jarang dipakai dan sampai saat ini belum pernah digunakan, sehingga detailnya belum dapat dijelaskan."),
            f("mdTask", "object", false, "Membuat task yang dinamis dari master data — data task diambil dari report dengan kriteria tanggal dan parameter tertentu, bukan task statis. Contoh: form dapat mengambil data dari Report \"Sales\" untuk bulan saat ini, lalu ditampilkan sebagai task list. Sampai saat ini belum pernah dipakai di level FORM."),
            f("queryFilters", "object", false, "Bisa digunakan untuk hanya menampilkan task dengan status tertentu, priority tertentu, atau kondisi spesifik lainnya. Detail dijelaskan pada [[queryfilters]]."),
            f("btnFilters", "array", false, "List kondisi untuk mengontrol kapan button/action ditampilkan di UI. Saat menampilkan task list, sistem mengecek apakah button harus ditampilkan. Struktur tiap object dijelaskan pada [[validationcondition]].", { of: "object" }),
            f("btnFilterAnd", "boolean", false, "Mode validasi untuk `btnFilters`: `false` → logika OR (cukup salah satu kondisi terpenuhi); `true` → logika AND (semua kondisi harus terpenuhi). Default-nya `false`."),
            f("needSplit", "boolean", false, "Flag untuk membagi/split 1 task menjadi multiple tasks berdasarkan data field tertentu. Default-nya `false`."),
            f("cloneTask", "boolean", false, "Jika diisi `true`, setiap submit akan menghasilkan (generate) 2 data history sekaligus: satu dengan `_vsb` bernilai `PAUSED` dan satu lagi `ACTIVE`. Dapat diaktifkan melalui `enableCloneTask` pada [[validation|Validation]] (paused). Default-nya `false`."),
            f("updateNumberAI", "array", false, "List berisi `key` dari collection [[number_ai|NUMBERAI]]. Digunakan untuk meng-update NUMBERAI: setiap `key` yang dimasukkan akan membuat `basedOnSubmit` pada dokumen NUMBERAI terkait diset menjadi `true` ketika form disubmit.", { of: "string" }),
            f("overrideTask", "object", false, "Konfigurasi untuk mengubah/override task lain setelah task saat ini (current) di-submit. Detail tiap field dan cara kerjanya dijelaskan pada [[overridetask|OverrideTask]]."),
            f("triggerViewField", "object", false, "Konfigurasi untuk menampilkan/menyembunyikan (show/hide) field secara dinamis berdasarkan nilai tertentu. Detail tiap field dan cara kerjanya dijelaskan pada [[triggerviewfield|TriggerViewField]]."),
            f("autoStockHandler", "object", false, "Konfigurasi untuk otomatis mengurangi stok dari report sesuai quantity yang digunakan saat task di-submit. Detail tiap field dan cara kerjanya dijelaskan pada [[autostockhandler|AutoStockHandler]]."),
            f("hideInHistory", "array", false, "List berisi NodeIdentity (node/form) yang disembunyikan dari history task.", { of: "object" }),
            f("workflowId", "string", false, "Reference ke `workflows._id`.", { depth: 1 }),
            f("nodeId", "string", false, "Reference ke `nodes.id`.", { depth: 1 }),
        ],
        example: {
            id: "form1",
            name: "Input Rekapitulasi Resign",
            iconURL: "https://portal.byonchat2.com/iconjaya/createKunjungan.png",
            order: 0,
            type: "FORM",
            _vsb: "ACTIVE",
            showBtns: ["CREATE_TASK"],
            locTitle: "Rekapitulasi Resign",
            submitType: "SUBMIT",
            groups: ["karyawan", "supervisoroutlet"],
            routing: {
                defaultDest: {
                    node: {
                        workflowId: "workflowResignImperial",
                        nodeId: "formApproval1",
                    },
                },
            },
            preview: {
                title: "By : ${workflowResignImperial.form1.userInfo.name}",
                subtitle: "${workflowResignImperial.form1.formData.brand}",
            },
            assignment: { type: "ALL" },
            locRequired: false,
            locPreview: false,
            fields: [],
            reportAccess: { byUserGroups: { "*": "ALL" } },
            position: { x: 20, y: -20 },
        },
        indexes: [
            { name: "_id", keys: ["_id"], unique: true },
        ],
        relations: [
            { field: "nodes", to: "nodes", kind: "belongs to" },
            { field: "routing.defaultDest.node.workflowId", to: "workflows", kind: "references" },
            { field: "routing.defaultDest.node.nodeId", to: "nodes", kind: "references" },
            { field: "hideInHistory[].workflowId", to: "workflows", kind: "references" },
            { field: "hideInHistory[].nodeId", to: "nodes", kind: "references" },
            { field: "preview.searches[].formId", to: "nodes", kind: "references" },
            { field: "preview.searches[].key", to: "fields", kind: "references" },
            { field: "updateNumberAI[]", to: "number_ai", kind: "references" },
        ],
    },

    assignment: {
        section: "Workflows",
        description: "Assignment adalah object untuk menentukan siapa yang bisa mengakses/mengerjakan task di sebuah FORM.",
        long: "Assignment merupakan turunan dari [[form|FORM]] yang menentukan siapa assignee dari sebuah task. Penentuan assignee dapat berdasarkan berbagai kondisi: dari field, dari group, dari parameter, atau di-split ke banyak orang.",
        meta: { documents: "—", indexed: false },
        notes: [
            { kind: "note", text: "Tipe Assignment (`type`): `ALL` (default) — semua user bisa akses task; `DIRECT` — user tertentu saja; `MENTIONED` — user yang di-mention; `BY_PARAM` — berdasarkan parameter user management; `BY_FORM` — berdasarkan form tertentu." },
            { kind: "tip", text: "`Assignment.all()` membuat assignment dengan type `ALL`, artinya task dapat diakses oleh semua user." },
        ],
        fields: [
            f("type", "string", true, "Tipe assignment — menentukan cara penentuan assignee.", {
                enumValues: ["ALL", "DIRECT", "MENTIONED", "BY_PARAM", "BY_FORM"],
            }),
            f("refNode", "object", false, "Referensi node/form lain untuk menentukan assignee."),
            f("refField", "string", false, "Referensi field dalam task untuk mengambil data assignee."),
            f("refGroup", "string", false, "Referensi user group untuk assignment by group."),
            f("useByFromIfReject", "boolean", false, "Jika task di-reject, task kembali ke assignee asal (`true` = ya)."),
            f("params", "object", false, "Parameter untuk menentukan assignee (HashMap<String, Object>), digunakan saat type `BY_PARAM`."),
            f("ops", "object", false, "Operasi kriteria untuk filter/validasi assignment (HashMap<String, CriteriaOperation>)."),
            f("split", "boolean", false, "Flag apakah task akan di-split ke multiple assignee."),
        ],
        example: {
            type: "ALL",
        },
        example2Label: "Contoh `DIRECT` — task hanya bisa diakses oleh user tertentu.",
        example2: {
            type: "DIRECT",
        },
        example3Label: "Contoh `BY_PARAM` — assignee ditentukan dari parameter user management, di sini nilai `branch` diambil dari field pada form sebelumnya (`form2`) via replacer.",
        example3: {
            type: "BY_PARAM",
            params: {
                branch: "${$.form2.formData.branch}",
            },
            useByFromIfReject: false,
        },
        indexes: [],
        relations: [],
    },

    fields: {
        section: "Workflows",
        description: "Fields adalah bagian paling kompleks dari FORM — di sinilah isi sekaligus tampilan dari sebuah form didefinisikan.",
        long: "Fields merupakan turunan dari [[form|FORM]] dan menjadi bagian paling kompleks, karena di sinilah isi dari sebuah FORM didefinisikan beserta tampilannya. Sebuah field dapat berupa berbagai jenis input, seperti `text`, `number`, `dropdown`, `datetime`, dan lainnya. Setiap field memiliki `key` unik yang dapat dirujuk oleh konfigurasi lain (misalnya `preview.searches`). (Detail tiap jenis field akan dilengkapi kemudian.)",
        meta: { documents: "—", indexed: false },
        notes: [],
        fields: [
            f("key", "string", true, "Key unik dari field di dalam form."),
            f("type", "string", true, "Jenis input field — menentukan tampilan/komponen field di form. Tiap jenis dijelaskan pada halaman turunannya di sidebar.", {
                enumValues: ["DROPDOWN", "LABEL", "CHECKBOX", "RADIO", "TEXT", "TEXT_AREA", "TEXT_NUMBER", "TEXT_JSON", "DECIMAL", "EMAIL", "NUMBER", "MAP", "OFFICER_SELECT", "RATE", "PHONENUMBER", "CURRENCY", "DATETIME", "DATE_MULTIPLE", "IMAGE", "IMAGE_KIT", "VIDEO", "AUDIO", "IMAGE_DRAW", "SCAN_QR", "LOUNGE_FILE", "NESTED_DROPDOWN", "ITEM_LIST", "FORM_CHILD", "CARD_LIST", "AUDIT", "AUDIT_WEB", "CONTACT", "DIFFERENT_VIEW", "NFC"],
            }),
        ],
        example: {
            key: "userInfo.name",
            type: "TEXT",
            "...": "...",
        },
        indexes: [],
        relations: [],
    },

    queryfilters: {
        section: "Workflows",
        description: "QueryFilters digunakan untuk memfilter task yang ditampilkan pada sebuah FORM.",
        long: "QueryFilters merupakan turunan dari [[form|FORM]] yang dapat digunakan untuk hanya menampilkan task dengan status tertentu, priority tertentu, atau kondisi spesifik lainnya. QueryFilters terbagi menjadi tiga kelompok berdasarkan status task: `news`, `onProgress`, dan `historys`.",
        meta: { documents: "—", indexed: false },
        notes: [],
        fields: [
            f("news", "array", false, "Filter untuk task BARU (status: TODO). Isi tiap object dijelaskan pada [[validationcondition]].", { of: "object" }),
            f("onProgress", "array", false, "Filter untuk task SEDANG DIKERJAKAN (status: PENDING). Isi tiap object dijelaskan pada [[validationcondition]].", { of: "object" }),
            f("historys", "array", false, "Filter untuk task SELESAI (status: DONE). Isi tiap object dijelaskan pada [[validationcondition]].", { of: "object" }),
        ],
        example: {
            news: [{ "...": "..." }],
            onProgress: [{ "...": "..." }],
            historys: [{ "...": "..." }],
        },
        indexes: [],
        relations: [],
    },

    overridetask: {
        section: "Workflows",
        description: "OverrideTask adalah konfigurasi untuk mengubah/override task lain setelah task saat ini (current) di-submit.",
        long: "OverrideTask merupakan turunan dari [[form|FORM]]. Saat sebuah task berhasil di-submit, sistem mengecek apakah form memiliki `overrideTask`; jika ada, task target (ditentukan oleh kombinasi `workflowId` dan `taskId`) akan di-override sesuai mode `dos`. Contoh penggunaan nyata terdapat pada konfigurasi client Aditama.",
        meta: { documents: "—", indexed: false },
        notes: [
            { kind: "tip", text: "Cara kerja singkat: ketika current task di-submit, jika `form.overrideTask` tidak `null` maka proses override dijalankan terhadap task target." },
            { kind: "note", text: "Mode 1 — DOS (`dos = true`): task target di-mark DONE. Progress task target di-reset dan `currentNode` dikosongkan, sehingga task otomatis selesai." },
            { kind: "note", text: "Mode 2 — SWIPE (`dos = false`): assignee task target diganti. Sistem mencari user baru berdasarkan `params`, `ops`, dan `overrideGroup`, lalu meng-assign task target ke user tersebut." },
            { kind: "note", text: "Contoh alur: Form men-submit task A → memicu override → task B di-mark DONE (mode DOS), atau task B di-assign ke user yang berbeda (mode SWIPE)." },
        ],
        fields: [
            f("taskId", "string", true, "Task ID dari task yang akan di-override. Nilainya bisa berupa variable/expression."),
            f("workflowId", "string", true, "Workflow ID dari task target — workflow tempat task yang akan di-override berada."),
            f("dos", "boolean", true, "Mode override: `true` = done (menandai task target selesai); `false` = swipe assignee (mengganti assignee task target)."),
            f("params", "object", false, "Parameter untuk mencari user baru (HashMap<String, Object>). Dipakai pada mode swipe (`dos = false`)."),
            f("ops", "object", false, "Operasi kriteria untuk seleksi/filter user (HashMap<String, CriteriaOperation>)."),
            f("overrideGroup", "string", false, "Group yang dipakai untuk menentukan assignee baru pada mode swipe."),
        ],
        example: {
            taskId: "${$.$.formData.taskId}",
            workflowId: "workflowJobTask",
            dos: false,
            params: {
                nik: "${SPLIT|${workflowOverrideTask.form1.formData.overrideEmployee}|__/__|1}",
            },
            overrideGroup: "technician",
        },
        indexes: [],
        relations: [
            { field: "workflowId", to: "workflows", kind: "references" },
        ],
    },

    triggerviewfield: {
        section: "Workflows",
        description: "TriggerViewField adalah konfigurasi untuk menampilkan/menyembunyikan (show/hide) field secara dinamis berdasarkan nilai tertentu.",
        long: "TriggerViewField merupakan turunan dari [[form|FORM]] yang berguna untuk membuat form dinamis: field yang ditampilkan bergantung pada pilihan/nilai yang dimasukkan user sebelumnya. Saat form dibuka, sistem mengevaluasi `valueToValidate`, lalu menampilkan field-field yang dipetakan pada nilai tersebut di dalam `fields`. Field lain yang tidak termasuk akan tetap disembunyikan (hidden).",
        meta: { documents: "—", indexed: false },
        notes: [
            { kind: "tip", text: "Kegunaan: membuat form dinamis — field yang muncul menyesuaikan pilihan/nilai user sebelumnya." },
            { kind: "note", text: "Cara kerja: saat form dibuka, sistem menghitung daftar field yang harus aktif dari `getKeyFieldsToBeView(taskProgress)`. Setiap field yang `key`-nya termasuk dalam daftar tersebut di-set `hide = false` (ditampilkan); sisanya tetap hidden." },
            { kind: "note", text: "Contoh: berdasarkan nilai `valueToValidate`, jika hasilnya `PP` → tampilkan field grup PP (`temperature`, `humidity`, dst); jika `Office` → tampilkan field grup Office (`color_a4`, `bw_a4`, dst); jika `Timbangan` → list kosong sehingga tidak ada field tambahan yang ditampilkan. Field lain tetap disembunyikan." },
        ],
        fields: [
            f("valueToValidate", "string", true, "Expression value yang akan dicek/dievaluasi. Nilai ini ditulis menggunakan [[replacer_overview|replacer]]."),
            f("fields", "object", true, "Mapping `nilai → daftar key field`. Key map adalah kemungkinan nilai dari `valueToValidate`; value-nya adalah daftar `key` field yang akan ditampilkan ketika nilai tersebut cocok. List kosong berarti tidak ada field tambahan yang ditampilkan."),
        ],
        example: {
            valueToValidate: "${IFNULL|${workflowMaintenance.form4.formData.cm1}|${workflowMaintenance.form5.formData.cm1}}",
            fields: {
                PP: ["temperature", "humidity", "voltageInput", "voltageOutput", "groundInput", "groundOutput", "electricInst", "clearence", "envCleanlines", "cyanToner", "magentaToner", "yellowToner", "blackToner"],
                Office: ["color_a4", "color_a3", "bw_a4", "bw_a3", "scan", "total", "action_taken", "toner_sb", "toner_sc", "toner_m", "toner_y"],
                Timbangan: [],
            },
        },
        indexes: [],
        relations: [
            { field: "fields", to: "fields", kind: "references (field keys)" },
        ],
    },

    autostockhandler: {
        section: "Workflows",
        description: "AutoStockHandler adalah konfigurasi untuk otomatis mengurangi stok dari report sesuai quantity yang digunakan saat task di-submit.",
        long: "AutoStockHandler merupakan turunan dari [[form|FORM]]. Ketika sebuah task di-submit, konfigurasi ini akan mengurangi (cut) stok pada report sumber sesuai quantity yang dipakai. Bila stok tidak mencukupi, submit ditolak dengan pesan `outOfStockMesssage`. Contoh: saat submit form pembelian bahan baku, stok pada inventory report otomatis berkurang sesuai quantity yang dibeli.",
        meta: { documents: "—", indexed: false },
        notes: [
            { kind: "tip", text: "Kegunaan: otomatis memotong stok dari report saat task di-submit, sehingga stok inventory selalu sinkron tanpa update manual." },
            { kind: "note", text: "Langkah 1 — Cek kondisi: jika `conditions` diisi, seluruh kondisi dievaluasi lebih dulu. Jika ada satu saja yang tidak valid, proses pemotongan stok dibatalkan." },
            { kind: "note", text: "Langkah 2 — Proses stok: jika lolos, sistem menjalankan pemotongan stok. Bila hasilnya berupa pesan (stok habis), submit ditolak (return error). Bila berhasil, hasilnya disimpan ke `formData` pada key `keyResult`." },
            { kind: "note", text: "Langkah 3 — Logika cutting: untuk tiap item, selama `mainStockCutQty` masih lebih dari 0, sistem mencari stok dari report lalu menguranginya sebanyak `min(stok, sisa qty)`. Jika stok report habis sebelum qty terpenuhi, dikembalikan pesan out of stock (`Stok tidak cukup`)." },
        ],
        fields: [
            f("conditions", "array", false, "Daftar kondisi yang harus terpenuhi sebelum proses pemotongan stok dijalankan. Struktur tiap object dijelaskan pada [[validationcondition]].", { of: "object" }),
            f("outOfStockMesssage", "string", false, "Pesan yang ditampilkan ketika stok habis/tidak cukup."),
            f("ilCacheData", "string", false, "Data item/bahan (ingredients) yang diambil dari item list."),
            f("mainStockCutQty", "string", true, "Menentukan field quantity mana yang nilainya dipakai sebagai jumlah stok yang dipotong."),
            f("dataToSave", "array", false, "Daftar field dari report yang ikut disimpan setelah proses.", { of: "string" }),
            f("reportStockQty", "string", true, "Field yang menyimpan jumlah stok pada report."),
            f("reportType", "string", true, "Tipe report yang dipakai sebagai sumber stok (mengacu pada [[reports|Reports]])."),
            f("collection", "string", true, "Nama collection tempat data stok disimpan."),
            f("params", "object", false, "Filter untuk mencari data stok pada report."),
            f("keyResult", "string", false, "Key pada `formData` tempat hasil proses disimpan."),
        ],
        example: {
            conditions: [{ "...": "..." }],
            outOfStockMesssage: "Stok tidak cukup",
            ilCacheData: "items",
            mainStockCutQty: "qtyUsed",
            dataToSave: ["batchNo", "expDate"],
            reportStockQty: "stockQty",
            reportType: "STOCK",
            collection: "inventory",
            params: { "...": "..." },
            keyResult: "autoStockResult",
        },
        indexes: [],
        relations: [
            { field: "conditions", to: "validationcondition", kind: "references (many)" },
        ],
    },

    validation: {
        section: "Workflows",
        description: "Validation adalah salah satu jenis Nodes yang berperan sebagai titik percabangan (decision) — mengarahkan alur ke node berikutnya berdasarkan kondisi, tanpa menampilkan form ke user.",
        long:
            "Validation merupakan salah satu jenis [[nodes|Nodes]] pada Workflows, di samping [[form|FORM]]. Bedanya, FORM menampilkan form untuk diisi user, sedangkan Validation tidak punya tampilan — ia adalah node percabangan yang menentukan ke mana alur akan berjalan selanjutnya.\n\n" +
            "Sebuah Node dikenali tipenya lewat field `type`. Ketika `type` bernilai `VALIDATION`, sistem memproses node tersebut sebagai titik keputusan, bukan sebagai form.\n\n" +
            "Cara kerjanya seperti blok `if / else if / else` pada sebuah flowchart. Sistem mengevaluasi daftar kondisi pada `routes` satu per satu secara berurutan. Kondisi pertama yang bernilai `true` menentukan node tujuan (`dest`), dan sisanya diabaikan. Apabila tidak ada satu pun kondisi yang terpenuhi, alur jatuh ke `defaultDest`.\n\n" +
            "Tiap kondisi di `routes` mengikuti struktur [[validationcondition|ValidationCondition]] yang membandingkan `field op value`, namun di sini setiap kondisi juga membawa `dest`-nya sendiri sebagai tujuan rute. Karena seluruh percabangan ini didefinisikan sebagai data (JSON), alur workflow dapat dirancang dan diubah tanpa mengubah kode program.",
        meta: { documents: "—", indexed: true },
        notes: [
            { kind: "note", text: "Evaluasi `routes` berjalan berurutan dari atas ke bawah. Kondisi pertama yang bernilai `true` menang — sisanya tidak dievaluasi lagi. Jika tidak ada yang cocok, alur memakai `defaultDest`. Karena itu, urutan kondisi penting: letakkan kondisi yang lebih spesifik di atas." },
            { kind: "note", text: "Node Validation tidak menampilkan UI apa pun. Ia hanya menentukan rute, sehingga tidak memiliki `fields`, `assignment`, atau atribut tampilan seperti pada [[form|FORM]]." },
            { kind: "note", text: "`dest.node` bersifat nullable. Apabila diisi `null` (mis. `\"dest\": { \"node\": null }`), artinya tidak ada node berikutnya → **alur berhenti/selesai (END)** ketika rute itu diambil. Contoh umum: status `Reject` yang tidak perlu dilanjutkan." },
            { kind: "note", text: "`field` pada tiap route biasanya berupa template variabel `${...}` yang di-resolve saat runtime. Contoh `${workflowHarianReal.formApproval.formData.status}` membaca nilai field `status` dari node form `formApproval` — pola: `<workflowId>.<nodeId>.formData.<key>`." },
            { kind: "warn", text: "Setiap kali mengubah `routes` atau `defaultDest`, Anda wajib melakukan [[restart_jar|Restart Jar]] di server. Tanpa restart, perubahan tidak akan terbaca oleh project Spring Boot." },
            { kind: "tip", text: "Bayangkan seperti tangga keputusan. Jika kondisi pertama terpenuhi, alur menuju node tujuannya. Jika tidak, dicek kondisi berikutnya, dan seterusnya. Bila tidak ada satu pun yang terpenuhi, alur menuju node default. Setiap kondisi tadi adalah satu entry pada `routes`, sedangkan node default adalah `defaultDest`." },
        ],
        fields: [
            f("id", "string", true, "Identitas node di dalam Workflows. Direferensikan oleh `dest.node.nodeId` dari node lain saat merutekan alur ke node ini."),
            f("name", "string", true, "LABEL/nama node Validation. Hanya untuk keterbacaan manusia — tidak memengaruhi logika."),
            f("type", "string", true, "Jenis Nodes. Untuk node Validation selalu diisi `VALIDATION`. Nilai inilah yang membuat sistem memproses node ini sebagai titik percabangan.", { enumValues: ["FORM", "VALIDATION"] }),
            f("routes", "array", true, "Daftar kondisi percabangan. Tiap elemen mengikuti struktur [[validationcondition|ValidationCondition]] (`field op value`) ditambah `dest` sebagai node tujuan jika kondisi terpenuhi. Dievaluasi berurutan; yang pertama cocok yang menang.", { of: "object" }),
            f("field", "string", true, "Sisi kiri perbandingan — umumnya template `${...}` yang dibaca dari data task saat runtime.", { depth: 1, eg: { field: "${workflowHarianReal.formApproval.formData.status}" } }),
            f("op", "string", true, "Operator perbandingan. Detail lengkap di [[validationcondition|ValidationCondition]].", { depth: 1, enumValues: ["EQUALS", "NOT_EQUALS", "GREATER_THAN", "LOWER_THAN", "CONTAINS", "IS_NULL", "IS_NOT_NULL", "MD_EXISTS"] }),
            f("value", "string", false, "Sisi kanan perbandingan — nilai yang dibandingkan dengan `field`.", { depth: 1, eg: { value: "Approve" } }),
            f("dest", "object", true, "Node tujuan apabila kondisi pada baris `routes` ini terpenuhi.", { depth: 1 }),
            f("node", "object", false, "Identitas node tujuan. Bersifat nullable — apabila diisi `null`, alur selesai (END).", { depth: 2 }),
            f("workflowId", "string", true, "Reference ke `workflows._id`.", { depth: 3 }),
            f("nodeId", "string", true, "Reference ke `nodes.id`.", { depth: 3 }),
            f("isSendNotifNeeded", "boolean", false, "Jika `true`, notifikasi dikirim saat rute ini diambil. (Di JSON kadang muncul ganda sebagai `isSendNotifNeeded` dan `sendNotifNeeded` karena penamaan getter Lombok untuk tipe Boolean; yang dipakai kode adalah `isSendNotifNeeded`.)", { depth: 1 }),
            f("defaultDest", "object", true, "Tujuan fallback ketika tidak ada satu pun kondisi pada `routes` yang terpenuhi. Strukturnya sama dengan `dest`.", { eg: { defaultDest: { node: { workflowId: "workflowId", nodeId: "formDefault" } } } }),
            f("groups", "array", false, "Daftar grup (mis. untuk hak akses/penugasan). Kosong = tidak ada.", { of: "object" }),
            f("notifications", "array", false, "Daftar NotificationSetting yang dikirim saat node ini aktif. Kosong = tidak ada.", { of: "object" }),
            f("order", "number", false, "Urutan tampil Nodes. Apabila tidak diisi, nilai default-nya adalah 0 (urutan paling awal)."),
            f("enableCloneTask", "boolean", false, "Jika `true`, mengaktifkan mekanisme clone task (PAUSED) yang berpasangan dengan `cloneTask` pada [[form|FORM]]. Default-nya `false`."),
        ],
        example: {
            id: "valApproval",
            name: "Validasi Approval",
            type: "VALIDATION",
            groups: [],
            notifications: [],
            routes: [
                {
                    field: "${workflowHarianReal.formApproval.formData.status}",
                    op: "EQUALS",
                    value: "Approve",
                    dest: { node: { workflowId: "workflowHarianReal", nodeId: "filegen" } },
                    isSendNotifNeeded: true,
                    sendNotifNeeded: true,
                },
                {
                    field: "${workflowHarianReal.formApproval.formData.status}",
                    op: "EQUALS",
                    value: "Reject",
                    dest: { node: null },
                    isSendNotifNeeded: true,
                    sendNotifNeeded: true,
                },
                {
                    field: "${workflowHarianReal.formApproval.formData.status}",
                    op: "EQUALS",
                    value: "Revise",
                    dest: { node: { workflowId: "workflowHarianReal", nodeId: "valReject" } },
                    isSendNotifNeeded: true,
                    sendNotifNeeded: true,
                },
            ],
            defaultDest: { node: { workflowId: "workflowHarianReal", nodeId: "filegen" } },
        },
        indexes: [
            { name: "_id", keys: ["_id"], unique: true },
        ],
        relations: [
            { field: "nodes", to: "nodes", kind: "belongs to" },
            { field: "routes", to: "validationcondition", kind: "references" },
            { field: "routes.dest.node.workflowId", to: "workflows", kind: "references" },
            { field: "routes.dest.node.nodeId", to: "nodes", kind: "references" },
            { field: "defaultDest.node.nodeId", to: "nodes", kind: "references" },
        ],
    },

}
